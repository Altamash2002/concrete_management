<ul>
  <% @grades.each do |grade| %>
    <li><%= link_to grade.name, comparison_compare_path(building_id: params[:building_id], structure_id: @structure.id, grade_id: grade.id) %></li>
  <% end %>
</ul>